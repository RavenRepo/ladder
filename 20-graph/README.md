# 20-graph

The graph. `outcomes.jsonl` is append-only and is the entire learning substrate: one row per dispatch, capability, tier, gate verdict, failure class, cost. `substrate.json` is the last probe result. Both are generated — do not edit by hand.
